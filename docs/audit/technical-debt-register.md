# Technical Debt Register

Audit date: 2026-08-31

| ID | Severity | Repository | Area | Problem | Evidence | Risk | Recommendation |
| -- | -------- | ---------- | ---- | ------- | -------- | ---- | -------------- |
| TD-001 | BLOCKER | Both | Repository boundary | Token setup output is never consumed by the bot | `itobot-token-setup/token_manager.py:12-22`; `teamitobot-grant-tracker/config.py:18`; no cross-repo reference | Advertised combined setup workflow does not work | One canonical environment/provider-secret contract; consolidate repositories per ADR-001 |
| TD-002 | HIGH | Main | Grant identity | Persistence matches title only while application compares title + dates | `bot.py:406-424`; `database.py:333-380`; isolated probe produced one overwritten row | Recurring grant can be missed and history corrupted | Confirm natural key, migrate data/constraint, align all code paths |
| TD-003 | HIGH | Main | Transactions | Grant and per-user intents commit independently | `bot.py:438-452`; each DB method owns/commits a session | Crash can permanently omit notifications | One unit of work for grant + recipient intents |
| TD-004 | HIGH | Main | Delivery | Telegram send and sent marks have an unavoidable unmodeled gap | `bot.py:526-540`; marks/counters commit individually | Duplicate notifications after crash/DB failure | Explicit at-least-once contract, attempt state, classified bounded retries |
| TD-005 | HIGH | Main | Testing | No tests cover production bot behavior | No test files; pytest collected zero tests | Unsafe fixes/refactors and regressions | Stage 0 characterization, unit, PostgreSQL integration, and lifecycle tests |
| TD-006 | HIGH | Main | Schema management | `create_all` substitutes for migrations despite legacy column | `database.py:68-75`, `147-150`; no migration directory | Production schema drift and unsafe rollback | Baseline actual schema and add migration tooling before model changes |
| TD-007 | HIGH | Main | Error handling | Fatal errors are printed and swallowed | `bot.py:699-702`, `752-764`; `scraper.py:193-195` | Clean exit on failure; lost traceback/root cause | Structured exception logging; non-zero fatal exit; narrow catches |
| TD-008 | HIGH | Main | Deployment | Single-replica requirement is neither documented nor enforced | Polling/scheduler start unconditionally at `bot.py:678-686`; stats use read-modify-write | Multi-replica polling conflict, duplicates, lost counters | Enforce one replica; leader/claim locking only when required |
| TD-009 | MEDIUM | Main | Scraper | Missing details target leaves `apply_link` unbound and aborts cycle | `scraper.py:140-146`; reproduced probe returned `None` | One malformed card suppresses all discoveries | Initialize per card and isolate/reject malformed cards |
| TD-010 | MEDIUM | Main | Async runtime | Synchronous HTTP and DB work execute on event loop | `scraper.py:34-38`; async handlers call static synchronous `DB` methods | Telegram response stalls under latency/outage | Bounded worker thread initially or measured async adapter migration |
| TD-011 | MEDIUM | Main | Telegram formatting | Scraped title/URL inserted into Markdown without escaping | `bot.py:491-532` | Deterministic Telegram rejection and poison retries | Dedicated escaped renderer and length/batch tests |
| TD-012 | MEDIUM | Main | Retry policy | All Telegram exceptions treated alike and retried forever | `bot.py:547-552`; no attempt/error fields in `Notification` | Retry storms; blocked users and bad payloads never resolve | Classify transient/rate-limit/permanent outcomes, bounded persisted backoff |
| TD-013 | MEDIUM | Main | Health | Health returns 200 independent of app readiness | `bot.py:41-58` | False-green monitoring | Separate liveness/readiness with polling/task/scrape freshness state |
| TD-014 | MEDIUM | Main | Configuration | Numeric ranges not validated; unused settings suggest false capability | `config.py:31-34`; `ENVIRONMENT`/`LOG_LEVEL` have no callers | Scrape storm, bind failure, confusing operations | Typed immutable settings, range validation, explicit bootstrap |
| TD-015 | MEDIUM | Main | Time | Naive DB columns and inconsistent timezone interpretation | `database.py:46-49`, `77-81`, `122-126`; `bot.py:278-297` | Shifted timestamps and driver-dependent behavior | UTC-aware storage with tested migration; localize only in renderer |
| TD-016 | MEDIUM | Main | Privacy | Chat IDs are logged; user data has no retention/deletion flow | `bot.py:114`, `132`, `167`, `542-551`; `User` model `database.py:34-55` | Personal-data exposure/indefinite retention | Minimize/redact logs; document retention and deletion/deactivation |
| TD-017 | MEDIUM | Main | Dependencies | Manifest includes four known advisories | `requirements.txt:1,5`; 2026-08-31 pip-audit | Supply-chain baseline failure; future path could become exploitable | Tested upgrades to non-vulnerable versions; CI advisory scan |
| TD-018 | MEDIUM | Setup | Secret storage | Token JSON is plaintext with inherited permissions | `token_manager.py:92-116`; no chmod/ACL | Other local users/backups may read bot credential | Prefer provider secrets; owner-only permissions if file retained |
| TD-019 | MEDIUM | Both | Modules | Runtime concerns concentrated in god modules | Main `bot.py` 764 lines/`database.py` 825; setup `app.py` 583 | Broad change blast radius; weak test seams | Incrementally extract application use cases and adapters |
| TD-020 | MEDIUM | Both | CI/CD | No automated build/test/security/deploy validation | No `.github/workflows` in either repo | Broken code or vulnerable pins can merge/deploy | One consolidated CI pipeline and immutable release process |
| TD-021 | MEDIUM | Main | Polling policy | Startup discards pending Telegram updates | `bot.py:682-686`, `drop_pending_updates=True` | User commands sent during downtime are lost | Make policy explicit/tested; prefer preservation once handlers are idempotent |
| TD-022 | LOW | Main | Stats | Stats singleton and counters are not concurrency-safe | `database.py:710-772` queries first row and read-modify-writes | Duplicate stats rows/lost increments under multiple processes | Enforce one replica now; constraint/atomic updates if retained |
| TD-023 | LOW | Main | Dead code | Several DB methods and config values have no callers | `get_db`, `get_user`, notification count, delete helpers, user scrape/active helpers; `ENVIRONMENT`, `LOG_LEVEL` | Misleading supported surface | Confirm no external imports, then remove in Stage 6 |
| TD-024 | LOW | Main | Lifecycle | Health daemon is not stopped; application initialization is redundant/ambiguous | `bot.py:617-622`, `671-676`, `703-745` | Resource/lifecycle confusion during shutdown | One lifecycle owner; close health server and DB engine within deadline |
| TD-025 | LOW | Setup | Rate limiting | Process-local dictionary is unbounded by unique IP and unsynchronized | `app.py:153-190` | Memory/race risk if tool is exposed beyond loopback | Preserve loopback; bounded thread-safe design only if exposure is approved |
| TD-026 | LOW | Setup | Failure semantics | Telegram outage and invalid token both become HTTP 400 | `app.py:255-292`, `430-438` | Operator may replace a valid token during upstream outage | Return classified 502/503 for network/upstream failures; retain old token |
| TD-027 | LOW | Setup | Test maintainability | Test file is 2,248 physical lines and one test name is shadowed | `test_app.py:734`, `1929`; Ruff F811 | Hidden earlier test and hard navigation | Split by route/security/storage behavior; remove duplicate definition |
| TD-028 | LOW | Both | Dependency management | No Python/runtime lock, hashes, or dev/runtime grouping | Both `requirements.txt`; setup includes pytest at line 3 | Non-reproducible transitive builds and larger runtime set | One project config, supported runtime constraint, resolved lock, dev group |
| TD-029 | LOW | Main | Documentation | README lists nonexistent files and has a trailing code fence | `README.md:177-198`, `302` | Misleads operators/developers | Reconcile README with actual tree after architecture stabilizes |
| TD-030 | INFORMATIONAL | Setup | Security controls | CSRF, CSP, request-size limit, token non-disclosure, atomic replace are implemented and tested | `app.py:30-146`, `322-481`; `token_manager.py:62-127`; 83 tests pass | Valuable behavior could be lost during consolidation | Preserve tests/behavior if tool moves; do not rewrite unnecessarily |
| TD-031 | LOW | Main | Secret hygiene | Ignore rules do not cover all `.env.*` files | `.gitignore:1-3` ignores only `.env` and `.env.local` | A future environment-specific secret file could be committed | Ignore `.env.*` and allow only a redacted `.env.example` |
